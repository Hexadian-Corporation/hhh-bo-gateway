name: Issue closed

on:
  issues:
    types: [closed]

jobs:
  issue-closed:
    # Pinned SHA from Hexadian-Corporation/.github (branch: main). Bumps via Dependabot.
    uses: Hexadian-Corporation/.github/.github/workflows/issue-closed-reusable.yml@82c6bd07bfbf95b123f19d6a0da0dede44976f68
    secrets:
      PROJECT_TOKEN: ${{ secrets.PROJECT_TOKEN }}
